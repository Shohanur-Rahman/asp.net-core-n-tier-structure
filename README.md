#N Tier Architecture Using Asp.Net Core 5
UI = User Interface
BOL = Business Object Layer
BLL => Business Logic Layer
DAL = Data Access Layer
#WPIApp(UI) => ServiceManager(BOL) => BLL => Models(DAL)

